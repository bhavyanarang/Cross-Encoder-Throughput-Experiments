# @package _global_

defaults:
  - override /model_pool: default
  - override /batching: default
  - override /tokenizer_pool: default
  - override /server: default

name: "25a_tokenizer_pool_1w"
description: "Tokenizer pool baseline (1 worker) under high load"

model_pool:
  instances:
    - name: "cross-encoder/ms-marco-MiniLM-L-6-v2"
      backend: "mps"
      device: "mps"
      quantization: "fp16"
      compile_model: false
      max_length: 512

tokenizer_pool:
  enabled: true
  num_workers: 1

batching:
  enabled: false
  max_batch_size: 64
  timeout_ms: 50.0
  length_aware: false

experiment:
  batch_sizes: [32, 64]
  concurrency_levels: [8, 16, 32]
  benchmark_requests: 500
  warmup_iterations: 10
