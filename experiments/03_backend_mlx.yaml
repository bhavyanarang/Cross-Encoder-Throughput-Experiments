# Experiment 03: Backend Comparison - MLX
name: "03_backend_mlx"
description: "MLX backend with 16-bit quantization"

model:
  backend: "mlx"
  mlx:
    bits: 16
    group_size: 64

experiment:
  batch_sizes: [32]
  concurrency_levels: [1]
  benchmark_requests: 200
