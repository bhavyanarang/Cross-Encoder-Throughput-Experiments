# Experiment 13c: Multi-Model Pool - 4 Instances
name: "13c_multi_model_4x"
description: "Four model instances in pool for parallel processing"

model_pool:
  instances:
    - name: "cross-encoder/ms-marco-MiniLM-L-6-v2"
      backend: "mps"
      device: "mps"
      use_fp16: true
    - name: "cross-encoder/ms-marco-MiniLM-L-6-v2"
      backend: "mps"
      device: "mps"
      use_fp16: true
    - name: "cross-encoder/ms-marco-MiniLM-L-6-v2"
      backend: "mps"
      device: "mps"
      use_fp16: true
    - name: "cross-encoder/ms-marco-MiniLM-L-6-v2"
      backend: "mps"
      device: "mps"
      use_fp16: true
  routing_strategy: "round_robin"

batching:
  enabled: false

experiment:
  batch_sizes: [32]
  concurrency_levels: [4]
